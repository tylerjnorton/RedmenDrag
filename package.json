{
  "name": "redmen-drag",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:tylerjnorton/RedmenDrag.git",
  "scripts": {
    "build": "next build && next export -o build",
    "dev-bsb": "concurrently \"bsb -clean-world -make-world -w\" \"next dev\"",
    "dev": "next dev",
    "deploy": "next build && next export -o build && firebase deploy -m \"Commit: $(git rev-parse HEAD)\""
  },
  "dependencies": {
    "bs-next": "^3.0.0",
    "firebase-tools": "^4.0.2",
    "next": "^6.1.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "reason-react": "^0.4.2"
  },
  "devDependencies": {
    "bs-platform": "^4.0.2",
    "concurrently": "^3.6.1"
  }
}
